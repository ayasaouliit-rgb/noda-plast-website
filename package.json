{
  "name": "noda-plast-film-website",
  "version": "1.0.0",
  "private": true,
  "description": "NODA PLAST FILM website with server-side email forms",
  "main": "server.js",
  "scripts": {
    "start": "node --env-file=.env server.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "nodemailer": "^7.0.0"
  }
}
